# Repository-owned non-secret production defaults.
# Ansible supplies secrets and environment-specific allocations separately.

# Standalone Next.js / PM2 application settings.
# Public URL prefix, built into Next.js; use a leading slash and no trailing slash.
APP_BASE_PATH=/map
PM2_APP_NAME=indoor-mapping
